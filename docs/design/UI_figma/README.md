
  # MegaSena App Prototype

  This is a code bundle for MegaSena App Prototype. The original project is available at https://www.figma.com/design/ifae3XFPT9ioDZWmYddySu/MegaSena-App-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  